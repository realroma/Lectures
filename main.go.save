package main

import (
	"fmt"
	"project/Lectures/crawler"
)

func main() {
	a, err := crawler.Scan("https://go.dev", 1 )
	if err != nil {
		fmt.Println(err)
	}
	b, err := crawler.Scan("https://golang.org", 1)
	if err != nil {
		fmt.Println(err)
	}
	fmt.Println(a, b)
}
